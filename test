import java.awt.Color;
import java.awt.Graphics;
import java.awt.image.BufferedImage;
import java.io.File;
import java.io.IOException;

import javax.imageio.ImageIO;
import javax.swing.JFrame;
import javax.swing.JPanel;

public class TestGraphics extends JPanel
{

	public static void main(String[] args)
	{
		//dhmiourgoume to xarti
		TestGraphics kolla = new TestGraphics();
		
	}

	public TestGraphics()
	{
		//dhmiourgoume to para8yro
		JFrame parathyro = new JFrame();
		parathyro.setSize(860, 640);
		parathyro.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		
		parathyro.add(this);		
		this.setBackground(Color.PINK);
		parathyro.setVisible(true);
	}
	
	public void paint(Graphics g)
	{
		/*g.drawLine(0, 0, 100, 100);
		g.setColor(Color.GREEN);
		g.drawLine(100, 100, 200, 200);
		*/
		
		
		
		//startofcopypaste
		File file0 =new File("background.png");
		BufferedImage img0 = null;
		try {
			img0 = ImageIO.read(file0);
		} catch (IOException e) {
			e.printStackTrace();
		}
		g.drawImage(img0, 0 ,0 , this);
		//endofcopypaste
		
		
		
		File file1 = new File ("coffeWarriorMovment1ToR.png");
		BufferedImage img1 = null;
		try 
		{
			img1 = ImageIO.read(file1);
		} 
		catch (IOException e)
		{
			e.printStackTrace();
		}
		g.drawImage(img1, 100, 400, this);
	
		//end_of same code
		
				//startofcopypaste
				File file2 =new File("coffeEnemyMovment1ToL.png");
				BufferedImage img2 = null;
				try
				{
					img2 = ImageIO.read(file2);
				} catch (IOException e) {
					e.printStackTrace();
				}
				g.drawImage(img2, 150 ,400,75,115 , this);
				//endofcopypaste
				
				//προσπάθεια για : ολα τα I/O μέσα σε ΕΝΑ  Try-Catch
				//int[] myIntArray = new int[3];
				//int img[]=new int[10];
				/*for(int i=0;i<10;i++)  //προσπαθεια για ενα loop σε καθε frame
				{						//το i συμβολιζει ποσες εικονες θα φορτωθουν(πειραματικα βαζουμε ενα 10)
				
					
					try
					{
					img[i] =ImageIO.read(file0)
					}
					catch
					{
						
					}
				*/
				
				
						
	}

}
